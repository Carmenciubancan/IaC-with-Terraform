** Reusable building blocks for infrastructure. Instead of one giant main.tf file, split related resources into organized folders ***

***subnet/*** = Network setup (subnet, internet gateway, routing)
***webserver/*** = Server setup (EC2, security group, SSH key)
